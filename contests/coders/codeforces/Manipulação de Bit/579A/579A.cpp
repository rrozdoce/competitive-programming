#include <bits/stdc++.h>

using namespace std;

int main() {

    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int bin;
    cin >> bin;
    cout << __popcount(bin) << "\n";
    return 0;
}